// Copy this file to map-config.js and paste your Mapbox public token (pk.*).
// map-config.js is gitignored and never committed.
//
// Create a token at https://account.mapbox.com/access-tokens/
// Restrict it to your site URLs (e.g. overview-solutions.github.io, localhost).
window.MAPBOX_TOKEN = 'pk.your-public-token-here';
